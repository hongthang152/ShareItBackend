kind: Service
apiVersion: v1
metadata:
  name: ${_PROJECT_NAME}
spec:
  selector:
    app: ${_PROJECT_NAME}
  type: LoadBalancer
  ports:
  - protocol: http
    port: 80
    targetPort: ${_CONTAINER_PORT}